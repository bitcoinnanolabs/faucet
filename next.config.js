/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Adicione a nova configuração aqui:
  output: 'standalone' // Isso definirá o modo de saída do Next.js para 'standalone'
};

module.exports = nextConfig;